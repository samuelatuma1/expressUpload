<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body{
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            background-color: black;
            color: blanchedalmond;
        }
        .err{
            background-color: maroon;
            color : white;
            padding : 10px;
        }
    </style>
</head>
<body>
    <h1>Welcome to Picture Story . Sign Up here</h1>

    <!-- Errors from validationResult -->
    <% if (locals.errs) { %>
        <% for (let err of errs ) { %>
            <li class="err"><%= err.msg %></li>
        <% } %>
    <% } %>

    <!--Errors from dataBase-->
    <% if ( locals.dataBaseErr){ %>
        <li class="err"><%= locals.dataBaseErr %></li>
    <% } %>

    <!-- If success message-->
    <% if (locals.msg) {%>
        <li><%= locals.msg  %></li>
    <% } %>


    <!-- Form -->
    <form method="POST">
        <p>
            <label for="username">
                Username : <input type="text" name="username" id="username" minlength="2">
            </label>
        </p>
        <p>
            <label for="password">
                Password : <input type="text" name="password" id="password" minlength="3">
            </label>
        </p>

        <button>
            Sign Up
        </button>
    </form>
    <p>Already have an account ? <a href='./signin'>Sign In Here</a></p>
</body>
</html>